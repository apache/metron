
import {AlertSearchDirective} from './alert-search.directive';
describe('AlertSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertSearchDirective();
    expect(directive).toBeTruthy();
  });
});
