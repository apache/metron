# Months - two digi number is used
MONTH \d\d

# Days - two digit number is used
DAY \d\d

YEAR \d\d\d\d

# Time - two digit hour, minute, second and milliseconds as a decimal
TIME \d{2}:\d{2}:\d{2}(\.\d{0,3})?

# Timestamp - month, day, and time
TIMESTAMP %{YEAR:UNWANTED}-%{MONTH:UNWANTED}-%{DAY:UNWANTED}T%{TIME:UNWANTED}

# Generic word field
WORD \w+

# Priority
PRIORITY \d+

# Message - the message body of the log
MESSAGE .*

TAG .*?

EXTRAINFO .*?

# RFC3164 - the entire log message
UNIXSYSLOG5424 <%{PRIORITY:priority}>%{TIMESTAMP:timestamp_string}Z %{TAG:hostname} %{TAG:tag}: (\[%{EXTRAINFO:extra_info}\] )?%{MESSAGE:message}